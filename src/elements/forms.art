;; Forms
;; =====
;;
;; Related to forms submition.
;; For Html.art, those are injected into ``body``.

import ./{_generator}!

_generateElements [
    button      :default
    datalist    :default
    fieldset    :default
    form        :default
    input       :void
    label       :default
    legend      :default
    meter       :default
    optgroup    :default
    option      :default
    output      :default
    progress    :default
    select      :default
    textarea    :default
]


