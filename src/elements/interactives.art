;; Interactives
;; ============
;;
;; Some interactives elements.
;; For Html.art, those are injected into ``body``.

import ./{_generator}!

details: newElement 'details
dialog:  newElement 'dialog 
summary: newElement 'summary