;; Interactives
;; ============
;;
;; Some interactives elements.
;; For Html.art, those are injected into ``body``.

import ./{_generator}!

_generateElements [
    details :default
    dialog  :default
    summary :default
]