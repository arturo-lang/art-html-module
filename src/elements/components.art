;; Web Components
;; ==============
;;
;; A way to create custom components.
;; For Html.art, those are injected into ``body``.

import ./{_generator}!

_generateElements [
    slot        :default
    template    :default
]