;; Tables
;; ======
;;
;; Include data into table format.
;; For Html.art, those are injected into ``body``.


import ./{_generator}!

_generateElements [
    caption     :default
    col         :void
    colgroup    :default
    table       :default
    tbody       :default
    td          :default
    tfoot       :default
    th          :default
    thead       :default
    tr          :default
]
