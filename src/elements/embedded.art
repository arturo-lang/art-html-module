;; Embedded Content
;; ================
;;
;; For Html.art, those are injected into ``body``.

import ./{_generator}!

_generateElements [
    embed   :void
    iframe  :default
    object  :default
    picture :default
    portal  :default
    source  :void
    svg     :default
    math    :default
]